{{ for u in handler_usings }}
using {{ u }};
{{ end }}

namespace {{ namespace }};

public static class {{ handler_name }}
{
    public static {{ result_type }} Handle({{ message_name }} message{{ if has_handler_dependency }}, {{ handler_dependency_type }} {{ handler_dependency_param }}{{ end }})
    {
        // <architect:region name="handler-body">
        {{ handler_body }}
        // </architect:region>
    }
}
