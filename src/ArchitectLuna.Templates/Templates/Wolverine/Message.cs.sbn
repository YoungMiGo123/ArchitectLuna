namespace {{ namespace }};

public sealed record {{ message_name }}({{ for field in fields }}{{ field.type }} {{ field.name }}{{ if !for.last }}, {{ end }}{{ end }});

public sealed record {{ result_name }}({{ for field in result_fields }}{{ field.type }} {{ field.name }}{{ if !for.last }}, {{ end }}{{ end }});
