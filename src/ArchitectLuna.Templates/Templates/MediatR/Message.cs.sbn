using MediatR;
using {{ results_namespace }};

namespace {{ namespace }};

public sealed record {{ message_name }}({{ for field in fields }}{{ field.type }} {{ field.name }}{{ if !for.last }}, {{ end }}{{ end }}) : IRequest<{{ result_type }}>;
